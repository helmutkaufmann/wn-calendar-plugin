# top fields rendered in "outside"
fields:
  name:
    label: Name
    span: left
    required: true
  slug:
    label: Slug
    span: right
    required: true
    preset:
      field: name
      type: slug

# everything else goes under tabs (even if you have none today)
tabs:
  fields:
    # add other calendar fields here when you have them

