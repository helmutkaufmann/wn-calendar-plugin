fields:
    name:
        label: mercator.calendar::lang.calendar.name
        span: auto
    slug:
        label: mercator.calendar::lang.calendar.slug
        span: auto
        preset:
            field: name
            type: slug
entries:
    type: relation
    label: Entries
    style: preview
    # You can customize the list columns by creating and referencing a yaml file here
    # list: $/mercator/calendar/models/calendarentry/columns.yaml
